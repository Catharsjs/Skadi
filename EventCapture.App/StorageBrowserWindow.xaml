<Window x:Class="EventCapture.App.StorageBrowserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Change Storage"
        Width="820"
        Height="560"
        MinWidth="720"
        MinHeight="500"
        WindowStyle="None"
        ResizeMode="CanResize"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="True"
        Topmost="False"
        FontFamily="Segoe UI"
        FontSize="16"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">

    <Border x:Name="WindowFrame"
            Background="{StaticResource BackgroundBrush}"
            BorderBrush="{StaticResource AccentBrush}"
            BorderThickness="1"
            CornerRadius="4">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="54"/>
                <RowDefinition Height="68"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="42"/>
                <RowDefinition Height="66"/>
            </Grid.RowDefinitions>

            <Grid Background="{StaticResource SurfaceBrush}"
                  MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="48"/>
                </Grid.ColumnDefinitions>

                <Image Source="EventCapture.ico"
                       Width="28"
                       Height="28"
                       Margin="18,0,11,0"/>

                <StackPanel Grid.Column="1"
                            VerticalAlignment="Center">
                    <TextBlock Text="Change Storage"
                               FontSize="20"
                               FontWeight="SemiBold"/>
                    <TextBlock Text="Choose storage or enter the path"
                               Foreground="{StaticResource SecondaryTextBrush}"
                               FontSize="14"/>
                </StackPanel>

                <Button Grid.Column="2"
                        Click="Cancel_Click"
                        Content="×"
                        Background="Transparent"
                        Foreground="{StaticResource SecondaryTextBrush}"
                        BorderThickness="0"
                        FontSize="25"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="CloseBorder"
                                                Background="{TemplateBinding Background}">
                                            <TextBlock Text="{TemplateBinding Content}"
                                                       Foreground="{TemplateBinding Foreground}"
                                                       FontSize="{TemplateBinding FontSize}"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="CloseBorder"
                                                        Property="Background"
                                                        Value="{StaticResource HoverBrush}"/>
                                                <Setter Property="Foreground"
                                                        Value="{StaticResource PrimaryTextBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <Grid Grid.Row="1"
                  Margin="18,13">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="42"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="86"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="UpButton"
                        Style="{StaticResource BaseButtonStyle}"
                        Height="40"
                        Padding="0"
                        Content="↑"
                        FontSize="20"
                        ToolTip="Up one folder"
                        Click="Up_Click"/>

                <TextBox x:Name="PathBox"
                         Grid.Column="2"
                         Height="40"
                         Padding="11,7"
                         Background="{StaticResource SurfaceBrush}"
                         Foreground="{StaticResource PrimaryTextBrush}"
                         BorderBrush="{StaticResource BorderBrush}"
                         BorderThickness="1"
                         CaretBrush="{StaticResource AccentBrush}"
                         SelectionBrush="{StaticResource AccentBrush}"
                         FontSize="16"
                         VerticalContentAlignment="Center"
                         KeyDown="PathBox_KeyDown"
                         TextChanged="PathBox_TextChanged"/>

                <Button x:Name="GoButton"
                        Grid.Column="4"
                        Style="{StaticResource BaseButtonStyle}"
                        Height="40"
                        Content="Go"
                        FontSize="16"
                        Click="Go_Click"/>
            </Grid>

            <Grid Grid.Row="2"
                  Margin="18,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="190"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="3"
                        Padding="6">
                    <Border.Resources>
                        <DataTemplate x:Key="ExplorerNavigationItemTemplate">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="28"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Image Source="{Binding Icon}"
                                       Width="18"
                                       Height="18"
                                       HorizontalAlignment="Left"/>
                                <TextBlock Grid.Column="1"
                                           Text="{Binding DisplayName}"
                                           FontSize="14"
                                           TextTrimming="CharacterEllipsis"/>
                            </Grid>
                        </DataTemplate>

                        <Style x:Key="ExplorerNavigationItemStyle"
                               TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="8,5"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="ItemBorder"
                                                Background="Transparent"
                                                CornerRadius="3"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ItemBorder"
                                                        Property="Background"
                                                        Value="{StaticResource HoverBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="ItemBorder"
                                                        Property="Background"
                                                        Value="{StaticResource BorderBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>

                        <Style x:Key="ExplorerExpanderStyle"
                               TargetType="Expander">
                            <Setter Property="IsExpanded" Value="True"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Expander">
                                        <StackPanel>
                                            <ToggleButton x:Name="HeaderButton"
                                                          Height="30"
                                                          Padding="5,0"
                                                          Background="Transparent"
                                                          BorderThickness="0"
                                                          Cursor="Hand"
                                                          IsChecked="{Binding IsExpanded,
                                                              RelativeSource={RelativeSource TemplatedParent},
                                                              Mode=TwoWay}">
                                                <ToggleButton.Template>
                                                    <ControlTemplate TargetType="ToggleButton">
                                                        <Border x:Name="HeaderBackground"
                                                                Background="Transparent"
                                                                CornerRadius="3"
                                                                Padding="{TemplateBinding Padding}">
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="18"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock x:Name="Chevron"
                                                                           Text="&#xE76C;"
                                                                           FontFamily="Segoe MDL2 Assets"
                                                                           FontSize="11"
                                                                           Foreground="{StaticResource SecondaryTextBrush}"/>
                                                                <ContentPresenter Grid.Column="1"
                                                                                  Content="{Binding Header,
                                                                                      RelativeSource={RelativeSource AncestorType=Expander}}"
                                                                                  VerticalAlignment="Center"/>
                                                            </Grid>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="HeaderBackground"
                                                                        Property="Background"
                                                                        Value="{StaticResource HoverBrush}"/>
                                                            </Trigger>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="Chevron"
                                                                        Property="Text"
                                                                        Value="&#xE70D;"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </ToggleButton.Template>
                                            </ToggleButton>
                                            <ContentPresenter x:Name="ExpandSite"
                                                              ContentSource="Content"/>
                                        </StackPanel>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsExpanded" Value="False">
                                                <Setter TargetName="ExpandSite"
                                                        Property="Visibility"
                                                        Value="Collapsed"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Border.Resources>

                    <ScrollViewer x:Name="NavigationScrollViewer"
                                  VerticalScrollBarVisibility="Hidden"
                                  HorizontalScrollBarVisibility="Disabled"
                                  PreviewMouseWheel="NavigationScrollViewer_PreviewMouseWheel">
                        <StackPanel>
                            <Expander Style="{StaticResource ExplorerExpanderStyle}"
                                      IsExpanded="True">
                                <Expander.Header>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="24"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="&#xE734;"
                                                   FontFamily="Segoe MDL2 Assets"
                                                   FontSize="14"
                                                   Foreground="{StaticResource AccentBrush}"/>
                                        <TextBlock Grid.Column="1"
                                                   Text="Quick access"
                                                   FontSize="14"
                                                   FontWeight="SemiBold"/>
                                    </Grid>
                                </Expander.Header>
                                <ListBox x:Name="QuickAccessList"
                                         Margin="18,0,0,0"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         Foreground="{StaticResource PrimaryTextBrush}"
                                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                         ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                         ItemTemplate="{StaticResource ExplorerNavigationItemTemplate}"
                                         ItemContainerStyle="{StaticResource ExplorerNavigationItemStyle}"
                                         SelectionChanged="QuickAccessList_SelectionChanged"/>
                            </Expander>

                            <Expander Margin="0,6,0,0"
                                      Style="{StaticResource ExplorerExpanderStyle}"
                                      IsExpanded="True">
                                <Expander.Header>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="24"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="&#xE7F4;"
                                                   FontFamily="Segoe MDL2 Assets"
                                                   FontSize="14"
                                                   Foreground="{StaticResource AccentBrush}"/>
                                        <TextBlock Grid.Column="1"
                                                   Text="This PC"
                                                   FontSize="14"
                                                   FontWeight="SemiBold"/>
                                    </Grid>
                                </Expander.Header>
                                <ListBox x:Name="ThisPcList"
                                         Margin="18,0,0,0"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         Foreground="{StaticResource PrimaryTextBrush}"
                                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                         ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                         ItemTemplate="{StaticResource ExplorerNavigationItemTemplate}"
                                         ItemContainerStyle="{StaticResource ExplorerNavigationItemStyle}"
                                         SelectionChanged="ThisPcList_SelectionChanged"/>
                            </Expander>
                        </StackPanel>
                    </ScrollViewer>
                </Border>

                <Border Grid.Column="2"
                        Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="3">
                    <Grid>
                        <TextBlock x:Name="EmptyFolderText"
                                   Text="This folder has no subfolders"
                                   Foreground="{StaticResource SecondaryTextBrush}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Visibility="Collapsed"/>

                        <ListBox x:Name="FolderList"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Foreground="{StaticResource PrimaryTextBrush}"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 MouseDoubleClick="FolderList_MouseDoubleClick">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="7,5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="34"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="▰"
                                                   Foreground="{StaticResource AccentBrush}"
                                                   FontSize="18"/>
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Name}"
                                                   FontSize="16"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="Margin" Value="4,2"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border x:Name="FolderBorder"
                                                        Background="Transparent"
                                                        CornerRadius="3">
                                                    <ContentPresenter/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="FolderBorder"
                                                                Property="Background"
                                                                Value="{StaticResource HoverBrush}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="FolderBorder"
                                                                Property="Background"
                                                                Value="{StaticResource BorderBrush}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>

                        <Border x:Name="AuthorizationPanel"
                                Panel.ZIndex="10"
                                Background="{StaticResource SurfaceBrush}"
                                Visibility="Collapsed">
                            <Grid Width="460"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="18"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="14"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="20"/>
                                    <RowDefinition Height="38"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="Authorization"
                                           FontSize="20"
                                           FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}"/>

                                <TextBlock x:Name="AuthorizationMessage"
                                           Grid.Row="1"
                                           Margin="0,7,0,0"
                                           Foreground="{StaticResource SecondaryTextBrush}"
                                           FontSize="14"
                                           TextWrapping="Wrap"/>

                                <TextBlock Grid.Row="3"
                                           Text="User name"
                                           Style="{StaticResource LabelStyle}"/>

                                <TextBox x:Name="AuthorizationUserName"
                                         Grid.Row="4"
                                         Height="40"
                                         Padding="10,7"
                                         Background="{StaticResource BackgroundBrush}"
                                         Foreground="{StaticResource PrimaryTextBrush}"
                                         BorderBrush="{StaticResource BorderBrush}"
                                         BorderThickness="1"
                                         CaretBrush="{StaticResource AccentBrush}"
                                         SelectionBrush="{StaticResource AccentBrush}"
                                         FontSize="16"
                                         VerticalContentAlignment="Center"
                                         TextChanged="AuthorizationField_Changed"/>

                                <TextBlock Grid.Row="6"
                                           Text="Password"
                                           Style="{StaticResource LabelStyle}"/>

                                <PasswordBox x:Name="AuthorizationPassword"
                                             Grid.Row="7"
                                             Height="40"
                                             Padding="10,7"
                                             Background="{StaticResource BackgroundBrush}"
                                             Foreground="{StaticResource PrimaryTextBrush}"
                                             BorderBrush="{StaticResource BorderBrush}"
                                             BorderThickness="1"
                                             CaretBrush="{StaticResource AccentBrush}"
                                             SelectionBrush="{StaticResource AccentBrush}"
                                             FontSize="16"
                                             VerticalContentAlignment="Center"
                                             PasswordChanged="AuthorizationField_Changed"/>

                                <TextBlock x:Name="AuthorizationError"
                                           Grid.Row="8"
                                           Margin="1,7,0,0"
                                           Foreground="{StaticResource DangerBrush}"
                                           FontSize="14"
                                           Visibility="Collapsed"/>

                                <Grid Grid.Row="10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="120"/>
                                    </Grid.ColumnDefinitions>

                                    <Button Grid.Column="1"
                                            Style="{StaticResource BaseButtonStyle}"
                                            Height="38"
                                            Content="Cancel"
                                            FontSize="16"
                                            Click="AuthorizationCancel_Click"/>

                                    <Button Grid.Column="3"
                                            Style="{StaticResource AccentButtonStyle}"
                                            Height="38"
                                            Content="Connect"
                                            FontSize="16"
                                            IsDefault="True"
                                            Click="AuthorizationConnect_Click"/>
                                </Grid>
                            </Grid>
                        </Border>
                    </Grid>
                </Border>
            </Grid>

            <Border Grid.Row="3"
                    Margin="18,8,18,0"
                    Background="{StaticResource SurfaceBrush}"
                    CornerRadius="3"
                    Padding="10,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Ellipse x:Name="StatusDot"
                             Width="7"
                             Height="7"
                             Margin="0,0,9,0"
                             Fill="{StaticResource SecondaryTextBrush}"/>
                    <TextBlock x:Name="StatusText"
                               Grid.Column="1"
                               Text="Choose a storage folder"
                               Foreground="{StaticResource SecondaryTextBrush}"
                               FontSize="14"
                               TextTrimming="CharacterEllipsis"/>
                </Grid>
            </Border>

            <Grid Grid.Row="4"
                  Margin="18,12,18,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="150"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="1"
                        Style="{StaticResource BaseButtonStyle}"
                        Height="38"
                        Content="Cancel"
                        FontSize="16"
                        IsCancel="True"
                        Click="Cancel_Click"/>

                <Button x:Name="UseStorageButton"
                        Grid.Column="3"
                        Style="{StaticResource AccentButtonStyle}"
                        Height="38"
                        Content="Use Storage"
                        FontSize="16"
                        IsDefault="True"
                        Click="UseStorage_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>
